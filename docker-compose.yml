services:
  manager:
    build: .
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ~/.invisible-browser-manager:/data
    environment:
      - AUTH_TOKEN=${AUTH_TOKEN:-}
      - RUNTIME_SERVICE_TOKEN=${RUNTIME_SERVICE_TOKEN:-}
